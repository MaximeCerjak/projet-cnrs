'use client'

// app/page.tsx — Scène Vitrine (page 0)
// Les composants sont tous dans le layout, cette page est volontairement vide.
// La logique d'affichage conditionnelle est gérée dans chaque composant
// via useNavigationStore().currentPage.

export default function Home() {
  return null
}